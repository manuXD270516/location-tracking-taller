const { Router } = require('express');
//const { GroupQuestionController } = require('../controllers');

const router = Router();

// API END-POINTS: GET


/* router.get('/', GroupQuestionController.getAllGroupsQuestion);
router.get('/:parentsId/parents', GroupQuestionController.getAllGroupsQuestionForParents);
 */


// API END-POINTS: POST
//router.post('/', ParentsController.registerParents);
// API END-POINTS: PUT

// API END-POINTS: DELETE
//router.delete('/:nannyId', nannyController.delete);
// API END-POINTS: PATCH

module.exports = router;
